import PageContainer from "@/layouts/PageContainer";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <PageContainer>
      
    </PageContainer>
  );
};

export default Home;
